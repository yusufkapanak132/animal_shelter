name: PHP Quality Checks & Deploy

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'

    - name: Install Composer Dependencies
      run: composer install --prefer-dist --no-progress

    - name: Run PHP Syntax Check
      run: find . -type f -name '*.php' ! -path './vendor/*' -exec php -l {} \;

    - name: Run Composer Security Audit
      run: composer audit

    - name: Run PHPUnit Tests
      run: vendor/bin/phpunit tests/

  # НОВАТА СЕКЦИЯ ЗА АВТОМАТИЧНО КАЧВАНЕ
  deploy:
    needs: test # Това гарантира, че кодът ще се качи САМО ако тестовете са зелени!
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'

    # Тук генерираме папката vendor, но БЕЗ тестовите библиотеки (за да е по-лека)
    - name: Build Production Vendor Folder
      run: composer install --no-dev --optimize-autoloader

    # Това изпраща файловете към InfinityFree
    - name: FTP Deploy to InfinityFree
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: /htdocs/ # Това е главната папка в InfinityFree
        exclude: |
          **/.git*
          **/.git*/**
          **/tests/**
          phpunit.xml
